import { FC } from 'react';
import { IDefaultComponentProps } from '../../../utils';
export interface Props extends IDefaultComponentProps {
    images?: any;
}
export declare const DefaultTestID = "vg-carousel";
export declare const Carousel: FC<Props>;
