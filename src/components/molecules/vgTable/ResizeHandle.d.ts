import { ReactElement } from 'react';
import { ColumnInstance } from 'react-table';
export declare const ResizeHandle: <T extends Record<string, unknown>>({ column, }: {
    column: any;
}) => ReactElement;
